<article class="paper-page paper-page--static">
  <header class="paper-page__header">
    <span class="note-label">PAGE / {{ page.title }}</span>
    <h1>{{ page.title }}</h1>
  </header>
  <div class="paper-content">{{ render(page.content) }}</div>
</article>
